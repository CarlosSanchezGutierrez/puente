# Admin detail pages

Internal detail routes:

```txt
/admin/vocacional/[id]
/admin/servicios/[id]

Purpose:

Review a single registration or request.
See complete message/context.
View contact details.
Update status from the detail page.
Return to the filtered dashboard manually.

These routes require admin access.