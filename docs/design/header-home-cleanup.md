# Header and home cleanup

Changes:

- Header simplified to wordmark, short nav labels and one CTA.
- Removed secondary tagline from header to reduce visual noise.
- Home "Que hacemos" section rewritten with fewer icons and less card-like decoration.
- Added a sober principles section focused on utility and scope.

Encoding rule:

Touched files are written as UTF-8 without BOM and source text avoids direct accented characters where possible.